# Solution Architecture Knowledge Base — Log

## [2026-08-31] ingest | AI Foundations / F1 Query-style system design scenarios

- User pasted a 5-scenario prompt set for "AI Foundations" system design interview tracks: distributed data platforms intersecting with AI (F1 Query-style federated query engines, vector-relational hybrids).
- Created concept: federated-query-engines (F1 Query architecture — table interleaving, distributed execution DAG, cost-based optimizer pushdown decisions, hedged requests/tail-latency handling for hybrid relational+lake+vector queries)
- Created scenario: ai-data-platform-system-design — 5 scenarios: multimodal RAG metadata store (ACL/vector consistency), high-throughput feature store on distributed SQL (read/write skew, hotspotting), federated query engine over heterogeneous AI sources, zero-downtime embedding-dimension schema migration, LLM inference workload isolation/resource quotas
- Updated: index.md (registered both new pages, rev 7)
- Notes: Cross-linked to existing concepts rather than re-deriving them (database-sharding, distributed-caching, rate-limiting, idempotency, distributed-consensus, bulkhead) — this track is specifically about combining those primitives under AI/ML workload shapes, not new primitives.

## [2026-08-19] ingest | SQL fundamentals reference (joins, GROUP BY/HAVING, N+1, composite keys)

- User asked what the N+1 problem is in SQL specifically (distinct from the existing API-composition N+1 coverage), and for one dedicated SQL basics page: joins (with ASCII pictures), duplicates, GROUP BY with WHERE, counting, composite relations, and a full basic-SQL interview question bank.
- Created concept: sql-fundamentals — a single running sample dataset (customers/orders, deliberately including an unmatched customer AND an orphan order so all 4 join types show real NULL-filling behavior) used consistently across: a JOIN "which zone survives" reference table, all 4 join types worked with actual result rows, self-join, the WHERE-vs-HAVING distinction grounded in SQL's logical execution order (FROM→WHERE→GROUP BY→HAVING→SELECT→DISTINCT→ORDER BY→LIMIT), finding duplicates (GROUP BY/HAVING for values, ROW_NUMBER()/PARTITION BY for actual duplicate rows), COUNT(*) vs COUNT(column) vs COUNT(DISTINCT), composite primary keys via a students/enrollments/courses many-to-many junction-table diagram, and a full SQL N+1 section (ORM lazy-loading root cause, JOIN fix, batched WHERE IN fix, eager-loading ORM hooks by name).
- Included a ~14-question basic SQL interview bank as requested: WHERE vs HAVING, INNER vs LEFT JOIN, UNION vs UNION ALL, DELETE vs TRUNCATE vs DROP, PK vs FK vs unique key, composite keys, finding duplicates, 2nd-highest-salary (3 solution variants), N+1, COUNT variants, correlated subqueries, 1NF/2NF/3NF, clustered vs non-clustered indexes.
- Updated: api-composition.md — cross-linked its existing (API-layer) N+1 section forward to this page's (SQL-layer) N+1 section, framing them explicitly as the same pattern one layer apart, matching the "two related but distinct layers" cross-linking style already used elsewhere in this KB (e.g. the AIOps/LLM-observability distinction). Updated index.md (rev. 6).
- Notes: every non-trivial ASCII diagram (the join-zone reference table, all 4 worked join result tables, the composite-key junction-table box diagram) was built and column-verified with a Python script before being pasted in, per the alignment-bug lesson from earlier sessions — none were hand-typed and eyeballed.

## [2026-08-18] ingest | HTTP status codes & headers reference (interview gap fill)

- User asked for dedicated interview coverage of "all HTTP return values," the HTTP handshake, and important HTTP headers. Audit found status codes only existed as a partial subset (12 codes) embedded inside rest-api-design-principles.md, headers had no dedicated page (scattered mentions in caching.md, rate-limiting.md, security-architecture.md, and the nginx example in sre/networking-fundamentals.md), and handshake mechanics stopped at TCP+TLS with no HTTP-layer (ALPN/H2 preface/QUIC) coverage.
- Created concept: http-status-codes — full 1xx-5xx reference with a concrete example per code, including the interview-trap distinctions (401 vs 403, 400 vs 422, 502 vs 504, 301/302's method-preservation ambiguity vs 307/308's guarantee) and the "success theater" (200 OK with an error body) anti-pattern.
- Created concept: http-headers — caching/conditional-request headers (ETag/If-None-Match, Last-Modified), content negotiation (Accept/Content-Type/Content-Encoding), full CORS preflight mechanics, security headers (HSTS/CSP/X-Content-Type-Options/X-Frame-Options/Referrer-Policy), auth (Authorization/WWW-Authenticate), proxy/forwarding headers (X-Forwarded-For/-Proto, the spoofing-trust caveat), and W3C Trace Context (traceparent/tracestate) — cross-linking rather than re-deriving Cache-Control (caching.md), Retry-After/RateLimit-* (rate-limiting.md), and Idempotency-Key (idempotency.md), which already covered those in depth.
- Updated: sre/concepts/networking-fundamentals.md — added a new "HTTP-Layer Handshake" section (ALPN protocol negotiation inside the TLS ClientHello/ServerHello, HTTP/2's connection preface + SETTINGS frame exchange, and HTTP/3/QUIC's combined transport+TLS handshake vs TCP+TLS's sequential RTTs) to complete the handshake mechanics this file already owns for TCP/TLS; refreshed the "what happens when you type a URL" Q&A to reference ALPN/H2 preface and added a direct "is there really a separate HTTP handshake" Q&A.
- Updated: rest-api-design-principles.md — trimmed its embedded status-code subset to a cross-link (avoiding duplication with the new dedicated page), added both new concepts to its Related line. Updated network-architecture-fundamentals.md's HTTP/3 cross-link to mention the new ALPN/H2-preface content. Updated index.md (rev. 5, 2 new concepts) and sre/index.md's networking-fundamentals summary.
- Notes: kept status codes and headers as two separate concept pages rather than one, per this vault's "one concept per page" convention — they're commonly asked about independently and a combined page would bury one under the other. Verified all new box-drawing diagrams for exact column alignment before finishing, per the alignment-bug lesson from the earlier Responsible AI ingest.

## [2026-08-16] ingest | Microsoft CoreAI Responsible AI — Principal SWE interview prep

- User provided a live Microsoft Careers JD URL (Principal Software Engineer - Responsible AI, CoreAI, req 1970393556753209, Mountain View, CA). The primary URL served portal config JSON, not rendered content — cross-verified role details via the LinkedIn mirror listing and Glassdoor summary (WebSearch) before writing anything.
- Created company page: companies/microsoft-coreai-responsible-ai — role snapshot, team ownership (CoreAI = RAI + Azure OpenAI + MaaS + Azure ML + Cognitive Services), the two-surface RAI framing (product-facing vs SDLC-facing governance) as the central interview-differentiation angle, likely Principal-band interview loop, Microsoft culture-signal STAR prep (growth mindset / customer obsessed / One Microsoft / D&I — explicitly not an Amazon-LP-style rubric), and practice questions spanning distributed systems, RAI governance, MLOps lifecycle, and behavioral.
- Created concept: azure-ai-content-safety — the concrete Microsoft product (harm categories/severity 0-2-4-6, Prompt Shields user/document attack detection, groundedness detection, protected material detection, custom categories) implementing the generic guardrail pipeline in ai-guardrails-and-safety; noted Azure OpenAI's default-on, not-fully-removable content filter as the platform-enforced-floor mechanism.
- Created concept: responsible-ai-sdlc-governance — the JD's most distinctive, easy-to-miss ask: governing AI-generated requirements/design docs/code in the org's own SDLC (provenance/disclosure, named human owner of record, dependency-hallucination/slopsquatting verification gate, elevated not lowered review bar to counteract automation bias, sampling audits), explicitly scoped as orthogonal to product-facing RAI governance.
- Updated: topics/ai-governance-responsible-ai.md and concepts/ai-guardrails-and-safety.md now cross-link to both new concepts and are tagged #ResponsibleAI. Updated index.md (rev. 4 — new Companies section, 2 new concept entries). Appended an 8-card "Microsoft Responsible AI (CoreAI)" section to flashcards.md. Updated root INTERVIEW_NAVIGATOR.md with a new path.
- Notes: All new and directly related pages tagged #ResponsibleAI per user request, so the tag is queryable/filterable across the vault. Deliberately did not duplicate the general guardrail/governance mechanics already in ai-guardrails-and-safety.md and ai-governance-responsible-ai.md — the two new concepts compose with those pages rather than re-explaining them.

## [2026-08-10] ingest | Tiered caching platform scenario (Tier-0, Netflix Online Data Stores JD)

- Created scenario: design-tiered-caching-platform — full system-design walkthrough for a platform team's multi-tier (in-memory + distributed) caching layer in front of Tier-0 services. Covers: two-tier model, abstraction/SDK layer over storage, zone-aware replication (grounded in the public EVCache/Memcached precedent), cost-efficiency levers at extreme scale, failure modes tying back to gutter pools and cross-region invalidation, trade-offs table, and interview angles including the build-vs-buy and open-source-stewardship framing.
- User input was a real Netflix "Online Data Stores" team JD — used as scenario context/flavor (quoted only in a paraphrased context blockquote), not as verified insider architecture detail; the EVCache reference is public (Netflix Technology Blog).
- Updated: concepts/caching.md and concepts/distributed-caching.md now cross-link to the new scenario ("Related: Full Platform-Team Scenario"). Updated index.md (new scenario entry, rev. 3).
- Notes: deliberately did not duplicate strategy/eviction material from caching.md or sharding/replication mechanics from distributed-caching.md — the new page composes both into one scenario rather than re-explaining them.

## [2026-07-28] ingest | REST API design, distributed caching, and hot-path-first system design methodology

- Created concept: rest-api-design-principles — resource modeling, HTTP method safety/idempotency table, idempotency keys for POST, status codes (incl. "success theater" anti-pattern), Richardson Maturity Model (Levels 0-3, HATEOAS), versioning strategies, cursor vs offset pagination, filtering/sparse fieldsets, standardized error format
- Created concept: distributed-caching — consistent hashing (vs naive mod-N), virtual nodes, client-side vs proxy-based (McRouter) sharding, leader-follower and multi-leader replication, cross-region consistency via remote invalidation markers, gutter pools, lease-based thundering-herd protection at distributed scale. Explicitly scoped as the "scaling a cache beyond one node" companion to the existing concepts/caching.md (which covers single-node strategies/eviction) — cross-linked both directions, no duplication.
- Created pattern: hot-path-first-design — full ingest of https://newsletter.systemdesignclassroom.com/p/a-good-system-design-tackles-down-the-hot-path-first, recreated as 5 ASCII architecture diagrams (basic read/write split, load-balanced split, outbox-driven write/invalidation path, full e-commerce architecture, endpoint classification matrix), the read/write and display-read/decision-read classification framework, the 5-step methodology (measure/classify/separate/connect invalidation/avoid over-engineering), and the "tool-collecting vs architecture-first" mindset framing. Cross-referenced (not duplicated) the existing patterns/outbox.md for the cache-invalidation mechanics.
- Created source: hot-path-first-system-design (newsletter summary, standard sources/ template)
- Updated: concepts/caching.md, concepts/api-gateway.md, patterns/outbox.md, topics/integration-patterns.md, scenarios/interview-questions.md (added a callout in the "How to Answer" methodology pointing to hot-path-first-design for the high-level-design step) — all cross-linked to the new pages. index.md registered all 4 new pages. flashcards.md appended a 10-card "REST API Design, Caching & Hot-Path System Design" section.
- Notes: User explicitly requested inferring content from the newsletter URL and recreating its architecture diagrams in-KB — done via WebFetch extraction, then redrawn as ASCII per this vault's diagram convention (not copied/embedded from the source).

## [2026-07-28] update | Networking gap check + SA-view network architecture page

- User asked whether TCP/IP, handshakes, SSL/TLS, and other networking concepts are covered for interviews. Audit found they ARE covered in depth already, but only in `sre/concepts/networking-fundamentals.md` (TCP 3-way handshake, TLS 1.2 vs 1.3 handshake, cert chains, forward secrecy, proxies, Zero Trust/SASE) and `sre/concepts/networking-troubleshooting.md` (ping/dig/curl/tcpdump/iptables) — none of it was linked from `solution-arch/`.
- Created concept: network-architecture-fundamentals — the SA design-time view (DNS/GSLB architecture and TTL trade-offs, CDN cache-key design and misconfiguration risk, HTTP/1.1 vs 2 vs 3/QUIC adoption trade-offs, VPC/subnet segmentation, security groups vs NACLs, VPC peering vs Transit Gateway, Private Link) — explicitly scoped to NOT duplicate the sre/ handshake-mechanics/CLI-troubleshooting content; cross-links to it instead.
- Updated: topics/security-architecture.md and concepts/load-balancing.md now link to sre/concepts/networking-fundamentals (and load-balancers.md) for protocol-level depth. Updated index.md with the new concept entry.
- Notes: Same cross-reference-don't-duplicate approach used for the ml/ track in the prior AI SA ingest — sre/ owns protocol mechanics + ops troubleshooting, solution-arch/ owns the design-time SA decision layer.

## [2026-07-27] ingest | AI Solution Architect track — Agentic AI, OpenAI, and general SA gap fill

- Created topics (×7): ai-solution-architecture (role/skill map, maturity model, prompting/RAG/fine-tuning/agentic decision framework), agentic-ai-architecture (agent loop, 6 design patterns, multi-agent, memory, guardrails, evals), openai-platform-architecture (Chat Completions vs Responses vs Assistants, function calling, structured outputs, fine-tuning, embeddings, moderation, Azure OpenAI vs direct, model selection), llm-application-architecture (enterprise integration view of RAG/LLMOps, context engineering, streaming, caching — explicitly scoped to not duplicate ml/concepts/rag or ml/patterns/rag-pattern), ai-governance-responsible-ai (AI risk categories, EU AI Act, SR 11-7, HIPAA, governance stack), enterprise-architecture-frameworks (EA vs SA vs TA, TOGAF ADM, cloud Well-Architected Frameworks, build vs buy — general SA gap, not AI-specific), cost-architecture-finops (FinOps lifecycle, cloud cost levers, LLM token cost levers ranked by impact — general SA gap, not AI-specific)
- Created concepts (×6): vector-databases (HNSW/IVF/PQ, pgvector vs dedicated), prompt-engineering-and-context-design (context window budget, caching-friendly ordering), function-calling-and-tool-use (tool contract, idempotency), ai-guardrails-and-safety (prompt injection defense-in-depth), model-context-protocol-mcp (N×M integration problem), llm-observability-and-evals (trajectory evals, LLM-as-judge, eval-gated deploys)
- Created patterns (×5): agentic-workflow-patterns (6 composable patterns + decision checklist), multi-agent-orchestration (supervisor/peer-handoff/blackboard topologies), human-in-the-loop (risk-tiered approval gates), rag-enterprise-integration (access-control-aware RAG, freshness strategy — explicitly the enterprise-integration counterpart to ml/patterns/rag-pattern), ai-gateway-pattern (LLM traffic chokepoint)
- Created scenarios (×3): design-enterprise-rag-system, design-agentic-customer-support-system, ai-solution-architect-interview-questions (28 Q&A covering role/skills, agentic AI, OpenAI platform, RAG, MCP, guardrails/governance, cost/EA trade-offs)
- Created diagrams (×1): agentic-ai-reference-diagrams (8 ASCII diagrams: agent loop, RAG pipeline, multi-agent supervisor, AI gateway, guardrail pipeline, HITL checkpoint, MCP layer, LLMOps deploy pipeline)
- Created source: building-effective-agents-anthropic (workflow vs agent distinction; basis for the complexity-spectrum framing used throughout)
- Updated: index.md (registered all 22 new pages), overview.md (added AI SA cross-links), topics/architectural-styles.md (added agentic control-flow cross-link), flashcards.md (appended 14-card "AI Solution Architecture, Agentic AI & OpenAI" section)
- Notes: Deliberately cross-referenced rather than duplicated the existing ml/ ML-engineering track (llm-fundamentals, rag, rag-pattern, rlhf, embeddings) — this track covers the enterprise/integration/governance/cost architecture view an AI Solution Architect owns, ml/ covers the algorithmic/ML-engineering depth. All new pages verified for wikilink resolution (no orphans, no broken links).

## [2026-04-22] update | Microservices architecture — gap fill

- Created topics: microservices (8 fallacies, principles, communication, data, observability, team topology, pitfalls)
- Created concepts: service-mesh (data/control plane, mTLS, Istio canary), service-discovery (client-side vs server-side, Kubernetes DNS)
- Created patterns: microservices-decomposition (by capability/DDD/volatility; anti-patterns; sizing), database-per-service (polyglot persistence; cross-service query strategies; saga/outbox reference), api-composition (sequential/parallel/scatter-gather; N+1 fix; vs CQRS)
- Updated scenarios: interview-questions — appended Q21–Q30 (distributed monolith, service boundaries, discovery, cross-service data, service mesh, saga walkthrough, choreography vs orchestration, mTLS auth, observability, cascading failure)
- Updated index: registered all 6 new files; updated interview-questions count
- Notes: All new pages interlinked with existing concepts (CQRS, saga, outbox, circuit-breaker, strangler-fig)

## [2026-04-22] ingest | Solution Architecture knowledge base — initial build

- Created: overview, index, log
- Topics (×7): architectural-styles, nfr-quality-attributes, scalability-and-reliability, data-architecture, integration-patterns, security-architecture, cloud-agnostic-principles
- Concepts (×13): cap-theorem, acid-vs-base, load-balancing, caching, message-queues, api-gateway, cqrs, event-sourcing, database-sharding, idempotency, rate-limiting, distributed-consensus (service-mesh stub)
- Patterns (×10): circuit-breaker, bulkhead, saga, outbox, strangler-fig, sidecar-ambassador, event-driven-architecture, bff, cqrs-event-sourcing, blue-green-canary
- Scenarios (×6): interview-questions (20 Q&A), design-url-shortener, design-rate-limiter, design-notification-system, monolith-to-microservices, high-availability-platform
- Diagrams (×2): c4-model (4 levels with ASCII), common-system-patterns (8 ASCII diagrams)
- Reference: flashcards (40+ Anki cards)
- Sources: designing-data-intensive-applications, clean-architecture
- Notes: Cloud-agnostic throughout; ASCII diagrams for all major patterns; interview Q&A covers fundamentals through senior-level
