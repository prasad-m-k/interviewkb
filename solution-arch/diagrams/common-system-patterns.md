# Common System Architecture Diagrams

ASCII reference diagrams for frequently-appearing architectural patterns in interviews and design docs.

---

## 1. Three-Tier Web Application

```
                         Internet
                             │
                    ┌────────▼────────┐
                    │   CDN / WAF     │  (static assets, DDoS protection)
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Load Balancer  │  (L7, SSL termination)
                    └────────┬────────┘
                             │
            ┌────────────────┼────────────────┐
            ▼                ▼                ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │  App Server 1│ │  App Server 2│ │  App Server 3│  (stateless)
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           └────────────────┼────────────────┘
                            │
                   ┌────────▼────────┐
                   │  Cache (Redis)  │  (sessions, hot data)
                   └────────┬────────┘
                            │
               ┌────────────┴────────────┐
               ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │  Primary DB      │      │  Read Replica(s) │
    │  (PostgreSQL)    │─────▶│  (async repl.)   │
    └──────────────────┘      └──────────────────┘
```

---

## 2. Microservices with API Gateway

```
                    Mobile / Web / Partner
                             │ HTTPS
                    ┌────────▼────────┐
                    │   API Gateway   │  (auth, rate limit, route)
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ User Service │    │Order Service │    │Product Svc   │
│  [Go/gRPC]   │    │  [Java]      │    │  [Node.js]   │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
┌──────▼───────┐    ┌──────▼───────┐    ┌──────▼───────┐
│  Users DB    │    │  Orders DB   │    │  Products DB │
│ [PostgreSQL] │    │ [PostgreSQL] │    │  [MongoDB]   │
└──────────────┘    └──────┬───────┘    └──────────────┘
                           │
                    ┌──────▼───────┐
                    │   Kafka      │  (async events)
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
       ┌──────────┐ ┌────────────┐ ┌──────────────┐
       │Inventory │ │Notification│ │  Analytics   │
       │  Service │ │  Service   │ │  (Flink/Spark│
       └──────────┘ └────────────┘ └──────────────┘
```

---

## 3. Event-Driven / CQRS System

```
                     Commands
                        │
               ┌────────▼────────┐
               │  Command Handler │
               │  (validates,    │
               │   emits events) │
               └────────┬────────┘
                        │
               ┌────────▼────────┐
               │   Event Store   │  (append-only)
               │ [Kafka/EventDB] │
               └────────┬────────┘
                        │
           ┌────────────┼─────────────────┐
           ▼            ▼                 ▼
   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
   │ Projection A │ │ Projection B │ │ Projection C │
   │  (Order view)│ │ (Dashboard) │ │(Search index)│
   └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
          │                │                │
   ┌──────▼───────┐ ┌──────▼───────┐ ┌──────▼───────┐
   │  Read DB     │ │   Redis      │ │Elasticsearch │
   └──────────────┘ └──────────────┘ └──────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │  Queries
                  ┌────────▼────────┐
                  │  Query Handler  │
                  └─────────────────┘
```

---

## 4. Global Multi-Region Architecture

```
Users (Global)
       │
       ▼
  DNS / GeoDNS ──▶ Route to nearest region
       │
       ├──────────────────────────┐
       ▼                          ▼
┌─────────────────┐      ┌─────────────────┐
│   Region: US    │      │  Region: EU     │
│  ┌───────────┐  │      │  ┌───────────┐  │
│  │  CDN Edge │  │      │  │  CDN Edge │  │
│  └─────┬─────┘  │      │  └─────┬─────┘  │
│        │        │      │        │        │
│  ┌─────▼─────┐  │      │  ┌─────▼─────┐  │
│  │  LB + App │  │      │  │  LB + App │  │
│  └─────┬─────┘  │      │  └─────┬─────┘  │
│        │        │      │        │        │
│  ┌─────▼─────┐  │      │  ┌─────▼─────┐  │
│  │ Primary DB│◀─┼──────┼─▶│ Replica DB│  │
│  └───────────┘  │      │  └───────────┘  │
└─────────────────┘      └─────────────────┘
         │                        │
         └──────────┬─────────────┘
                    ▼
           Cross-region replication
           (async — eventual consistency)
```

---

## 5. Data Pipeline Architecture

```
Sources:
  App Events ──┐
  DB Changes ──┤──▶ Kafka (raw events) ──▶ Stream Processor ──▶ Data Warehouse
  API Logs   ──┘      (Kafka Connect)       (Flink/Spark        (BigQuery /
  3rd Party ──────▶ Data Lake (S3)          Streaming)          Snowflake)
                    (raw, all formats)           │
                          │                      ▼
                          ▼               Analytical
                    dbt transforms        dashboards (BI)
                          │
                          ▼
                    Serving layer
                    (feature store,
                     ML models)
```

---

## 6. Saga — Order Fulfilment Flow

```
                    ┌──────────────────────────────────┐
                    │         Saga Orchestrator         │
                    └──────────────┬───────────────────┘
                                   │
    ┌──────────────────────────────┼─────────────────────────────┐
    │                             │                              │
    ▼                             ▼                              ▼
Success path:                                              Failure path:
[1] Create Order ──▶ [2] Reserve Stock ──▶ [3] Take Payment ──▶ [4] Ship
                                                  │ FAIL
                                      ┌───────────┘
                                      ▼
                              Compensate [2]: Release Stock
                              Compensate [1]: Cancel Order
```

---

## 7. Cache Hierarchy

```
User Request
     │
     ▼
Browser Cache (Cache-Control header) ── HIT ──▶ Return
     │ MISS
     ▼
CDN Edge Cache (Cloudflare / Fastly) ─── HIT ──▶ Return
     │ MISS
     ▼
API Gateway Cache ────────────────────── HIT ──▶ Return
     │ MISS
     ▼
App In-Process Cache (Caffeine/Guava) ── HIT ──▶ Return
     │ MISS
     ▼
Distributed Cache (Redis) ───────────── HIT ──▶ Return
     │ MISS
     ▼
Database (Source of Truth)
     │
     └──▶ Populate all cache layers above ──▶ Return
```

---

## 8. Circuit Breaker State Machine

```
         ┌──────────────────────────────────────────────┐
         │                                              │
         │   CLOSED ────failures > threshold─────▶ OPEN │
         │     ▲                                    │   │
         │     │     success threshold met          │   │
         │  HALF-OPEN ◀────────────timeout──────────┘   │
         │     │                                        │
         │     └──────failures────▶ OPEN (again)        │
         └──────────────────────────────────────────────┘

  CLOSED:    All requests pass through. Count failures.
  OPEN:      All requests fail fast (return error/cached). Wait for timeout.
  HALF-OPEN: Allow probe requests. Decide: close or reopen.
```

## Sources
- [[solution-arch/sources/designing-data-intensive-applications]]
- [[solution-arch/sources/clean-architecture]]
